# Supabase URL and publishable key for secure client-side authentication and database access.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Public app URL used for auth redirects and callback links.
APP_URL=http://localhost:3000
